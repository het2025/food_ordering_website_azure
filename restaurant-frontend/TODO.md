# TODO: Create Finance UI under Admin Heading

## Steps to Complete:
- [ ] Create `src/components/pages/FinancePage.jsx` with finance management UI (stats, transactions list, modals for add/edit/delete transactions, hover effects)
- [ ] Update `src/App.jsx` to import FinancePage and add route `/finance` under SidebarLayout
- [ ] Test the new Finance page by running the app and navigating to `/finance`

## Progress:
- [x] Create `src/components/pages/FinancePage.jsx` with finance management UI (stats, transactions list, modals for add/edit/delete transactions, hover effects)
- [x] Update `src/App.jsx` to import FinancePage and add route `/finance` under SidebarLayout
- [x] Test the new Finance page by running the app and navigating to `/finance`

# TODO: Create Subscription UI under Admin Heading

## Steps to Complete:
- [ ] Create `src/components/pages/SubscriptionPage.jsx` with subscription management UI (stats, subscriptions list, modals for add/edit/delete subscriptions, hover effects)
- [ ] Update `src/App.jsx` to import SubscriptionPage and add route `/subscriptions` under SidebarLayout
- [ ] Test the new Subscription page by running the app and navigating to `/subscriptions`

## Progress:
- [x] Create `src/components/pages/SubscriptionPage.jsx` with subscription management UI (stats, subscriptions list, modals for add/edit/delete subscriptions, hover effects)
- [x] Update `src/App.jsx` to import SubscriptionPage and add route `/subscriptions` under SidebarLayout
- [x] Test the new Subscription page by running the app and navigating to `/subscriptions`

# TODO: Create Coupon UI under Admin Heading

## Steps to Complete:
- [ ] Create `src/components/pages/CouponPage.jsx` with coupon management UI (stats, coupons list, modals for add/edit/delete coupons, hover effects and animations)
- [ ] Update `src/App.jsx` to import CouponPage and add route `/coupon` under SidebarLayout
- [ ] Test the new Coupon page by running the app and navigating to `/coupon`

## Progress:
- [x] Create `src/components/pages/CouponPage.jsx` with coupon management UI (stats, coupons list, modals for add/edit/delete coupons, hover effects and animations)
- [x] Update `src/App.jsx` to import CouponPage and add route `/coupon` under SidebarLayout
- [x] Test the new Coupon page by running the app and navigating to `/coupon`
